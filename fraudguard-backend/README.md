# FraudGuard Backend

FastAPI + PostgreSQL + Celery Backend.

## Models
- `Transaction`
- `FraudReport`
- `UPIProfile`
- `User`

To run the backend, simply use docker compose from the `/fraudguard-backend` directory or run `uvicorn main:app --reload`.
