# Contributing Guide

- Work only on the `develop` branch.
- Do NOT push directly to `main`.
- For any new feature or fix, create a new branch from `develop`.
- Ensure all CI tests pass before requesting a review.
- Tag the proper owners for backend, ML, or frontend reviews depending on the edited paths.
